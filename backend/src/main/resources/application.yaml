spring:
  application:
    name: elewa-backend

  datasource:
    url: jdbc:postgresql://localhost:5433/accessibledocs
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:2634Hp$$}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    show-sql: false
    properties:
      hibernate:
        format_sql: true

  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 10MB

security:
  jwt:
    secret: ${JWT_SECRET:change-me-in-production-must-be-at-least-32-chars!!}
    access-token-expiry-ms: 900000
    refresh-token-expiry-ms: 604800000
    cookie-name: elewa_access
    refresh-cookie-name: elewa_refresh

fastapi:
  base-url: ${FASTAPI_URL:http://localhost:8000}
  # Must match INTERNAL_SECRET in Alvin's .env — never commit the real value
  internal-secret: ${INTERNAL_SECRET:change-me-shared-secret}
  timeout-seconds: 30

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health,info

logging:
  level:
    com.elewa: DEBUG
    org.springframework.security: INFO