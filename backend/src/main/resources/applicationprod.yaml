spring:
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: update # Ensure this is what you want for prod
  datasource:
    url: postgresql://postgres.ptrwshzwyaxmjxqpcuus:[2634Hp$$HM22]@aws-1-eu-central-1.pooler.supabase.com:6543/postgres
    username: ${DB_USER}
    password: ${DB_PASSWORD}
    hikari:
      maximum-pool-size: 20

  security:
    oauth2:
      client:
        registration:
          google:
            client-id: ${GOOGLE_CLIENT_ID}
            client-secret: ${GOOGLE_CLIENT_SECRET}

app:
  auth:
    token-secret: ${JWT_SECRET}
  cors:
    allowed-origins: ${ALLOWED_ORIGINS} # Better to set this as a variable in Railway

logging:
  level:
    root: INFO
    org.springframework.security: DEBUG # Temporary: helps see why OAuth fails
